# Security Hardening Backlog — lidarr.plugin.common

_Generated: 2026-05-23 | Phase 1 security audit_

| ID | Dimension | Finding | Severity | File:Line | Recommendation | Tracking-issue-ref |
|----|-----------|---------|----------|-----------|---------------|-------------------|
| COM-001 | Auth & token storage | **Windows**: `FileTokenStore` sets no explicit ACL on the token file; only Unix gets `UserRead\|UserWrite` via `File.SetUnixFileMode`. On Windows, the token JSON inherits parent-directory DACL which may be group-readable under some Lidarr deployment topologies. | Medium | `src/Services/Authentication/FileTokenStore.cs:184-196` | After atomic rename on Windows, call `FileSecurity` to restrict DACL to owner-only (`System.IO.File.SetAccessControl`). | COM-001 |
| COM-002 | Auth & token storage | `TryMigrateToProtectedFormat` uses synchronous `JsonSerializer.Serialize` + `stream.Flush()` inside an otherwise-async code path (called from `LoadAsync`). If the migration write fails and leaves a `.tmp` file behind, that file contains unencrypted JSON. | Low | `src/Services/Authentication/FileTokenStore.cs:361-398` | Add a `finally` block to delete the temp path on migration failure; or run migration fully async via `SaveAsync`. | COM-002 |
| COM-003 | Auth & token storage | `NamedMutexScope` passes `cancellationToken` to a `Register` callback that calls `_mutex.ReleaseMutex()`. If cancellation fires while a load/save is mid-flight the mutex releases under the concurrent operation, allowing a second process to enter the critical section simultaneously. | Medium | `src/Services/Authentication/FileTokenStore.cs:270-278` | Remove the cancellation-triggered `ReleaseMutex`; instead, use `WaitOne(timeout, exitContext)` and propagate `OperationCanceledException` after the wait returns `false`. | COM-003 |
| COM-004 | Sensitive logging | `Sanitize.SafeErrorMessage` redacts `Bearer` and `token=` patterns but does NOT redact `Authorization:` header values, `X-Auth-Token`, or Qobuz `user_auth_token` query parameters when they appear in raw exception messages. | Medium | `src/Security/Sanitize.cs:124-143` | Add additional regex patterns for `Authorization:\s*\S+` and `user_auth_token=\S+`; also redact `x-api-key` header values. | COM-004 |
| COM-005 | Sensitive logging | `Sanitize.IsSafePath` only checks for `..` as a substring — it does NOT normalise the path before checking. Unicode look-alikes (e.g., `%2e%2e`, `..`), or Windows UNC `\\?` paths containing `..` after normalisation can bypass the guard. | High | `src/Security/Sanitize.cs:57-61` | Normalise to `Path.GetFullPath` before applying the `..` check; return `false` for any path that escapes a configured root after normalisation. | COM-005 |
| COM-006 | Path & file validation | `Sanitize.PathSegment` does not enforce a maximum segment length. A malicious artist/title exceeding 255 chars can produce a filename longer than the OS limit, causing silent truncation or an `IOException` that may be swallowed downstream. | Medium | `src/Security/Sanitize.cs:34-52` | Add a configurable `maxLength` overload (default 200 chars) matching `QobuzConstants.Download.MaxFolderNameLength`. | COM-006 |
| COM-007 | Retry abuse / DoS | `RetryPolicyOptions.Aggressive` sets `MaxRetries = 5` with `InitialDelay = 200 ms`. No per-call circuit-breaker interlock: if the same operation is invoked under multiple concurrent coroutines (e.g., a Lidarr batch search), the aggregate outbound request rate can spike to 5 × n concurrent callers before any rate limiting applies. | Medium | `src/Services/Resilience/RetryPolicy.cs:85-91` | Document that `Aggressive` is not safe for concurrent fan-out; add a concurrency guard or require callers to couple it with `AdaptiveConcurrencyManager`. | COM-007 |
| COM-008 | Sync-over-async | `StreamingPluginMixins.cs:47` calls `Task.Delay(waitTime).Wait()` — a synchronous block inside what may be a Lidarr sync callback. The `.Wait()` captures the thread-pool thread and on an ASP.NET-style synchronisation context can deadlock. | Medium | `ext shared copies: src/Services/StreamingPluginMixins.cs:47` | Replace with `await Task.Delay(waitTime)` or, if the containing method must be sync, use `Task.Run(() => Task.Delay(waitTime)).Wait()` with an explicit thread-pool hop. | COM-008 |
| COM-009 | Rate limiting | `UniversalAdaptiveRateLimiter` stores state in a `ConcurrentDictionary<string, ServiceRateLimiter>` keyed on the raw service name string. If two plugins register the same service name (e.g., both using `"Default"`), they share the same `ServiceRateLimiter` instance and inadvertently throttle each other. | Medium | `src/Services/Performance/UniversalAdaptiveRateLimiter.cs:83-103` | Scope the dictionary to the consuming plugin's assembly name or require callers to prefix the key (e.g., `"Tidalarr:Default"`). | COM-009 |
| COM-010 | Plugin-host trust boundary | `DataProtectionTokenProtector` uses reflection to load `Azure.Identity.DefaultAzureCredential` at runtime. If a malicious assembly in the same process impersonates the `Azure.Identity` namespace, the reflection lookup could load it. There is no assembly-level identity check (PublicKeyToken). | Low | `src/Security/TokenProtection/DataProtectionTokenProtector.cs:146-147` | Add a PublicKeyToken check when resolving the type via `Type.GetType`, or validate the assembly's `FullName` after load. | COM-010 |
| COM-011 | Network failure behaviour | `HttpFileDownloadService` resumes partial downloads via `Range` header but never verifies the final assembled content length against any server-supplied `Content-Length`. A truncated download silently produces a corrupt file. | High | `src/Services/Download/HttpFileDownloadService.cs:54-60` | After final byte is written, compare total written bytes to the sum of `existing + response.Content.Headers.ContentLength`. Throw `IOException` on mismatch. | COM-011 |
| COM-012 | Network failure behaviour | `FileTokenStore` temp file names use `Guid.NewGuid().ToString("n")` which is non-cryptographic (predictable on some platforms with known entropy). On a shared `/tmp` a concurrent process could pre-create the temp filename to cause a `CreateNew` failure. | Low | `src/Services/Authentication/FileTokenStore.cs:133` | Use `Path.GetTempFileName()`-style approach or add retry with a fresh GUID on `IOException` — which already exists (lines 153-179). Document why this is acceptable. | COM-012 |
