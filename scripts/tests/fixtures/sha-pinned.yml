# Test fixture: all Common refs are correctly SHA-pinned (40-char hex)
name: SHA-Pinned Fixture
on: workflow_dispatch

jobs:
  gates:
    uses: RicherTunes/Lidarr.Plugin.Common/.github/workflows/packaging-gates.yml@b9492e87502cfe93ec90709b387aea5bb079c698
    with:
      common-path: ext/Lidarr.Plugin.Common

  smoke:
    uses: RicherTunes/Lidarr.Plugin.Common/.github/workflows/multi-plugin-smoke-test.yml@a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2
    with:
      test_plugins: qobuzarr

  # Third-party action (not Common) — should be ignored by the lint
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v3
